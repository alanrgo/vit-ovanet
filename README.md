# vit-ovanet

This code aims to use OVANet with ViT. Code was adapted from https://github.com/YanzuoLu/MLNet. 


# Setup environment 

To ensure conda is running
`source ~/miniconda3/bin/activate`

Sets environment up according to `environment.yaml` file.
`conda env create -f environment.yaml`